# Advanced Sequential Modelling - Martedì 26 maggio 2026

Vogliamo capire come costruire reti ricorrenti che siano competitive con i trasformer

## State Space Models and Mamba (RNNs)

Se abbiamo grandi moli di dati vorremmo poter paralizzare il training, abbiamo a che fsre con LSSTM che fa retroporpagazione dei gradienti e prende decisioni su quanto successo nel passato, potrei dare un'importanza troppo grande a quello che sto osservando adesso e dimenticare quanto successo negli istanti precedenti. Le LSTM cercano di risolvere il problema di revisione passate

## State Space Models

Gli state space models vengono da control engineering, è una rappresentazione a spazio di stato. I modelli ad assunzioni di stato sono nmodelli in cui sono interessaqto a calcolare un output y(t) e lo faccio dipendere da un input u e da uno stato x la nostra hidden representation o hidden state. generalmente ho un equazione che mi dice per calcolare l'output y ho bisogno di prendere lo stato x applicare una trasformazione c più la matrice d per il vettore u. C'è dipendenza dal tempo ed anche le matrici dipendono dal tempo, il modo in cui analizzo x ed il modo in cui analizzo u possono cambiare nel tempo. 

Il puntino sopra la x rappresenta la derivata e ci dice come si evolve lo sstto nel tempo, il modo in cui cambia, la derivata è essa stessa una rappresentazione lineare, il vecchio stato e l'input cambiano il modo in cui lo stato cambia nel tempo.

Il grafico ci fa vedere le matrici A B C e D, u è il mio input, y è il mio output, ottengo la derivata x che mi permette di calcolare con l'integrazione passo da x primo ad x lo mopltiplico per c e gli sommo il prodotto di D per u. Siccome tutto è lineare potremmo pensare di fare in modo che D sia zero.

L'idea è questa, immaginiamo che voglia campionare e capire quale sia il valore di x, mi chiedo qual era il valore di x a tempo t primo, so che da t primo a t è passato un certo delta t, integro in quell'intervallo per ottenere il delta x da sommare all'x di prima per otteenere l'x corrrente.

Se mettessi d=0 la formula ricorda un po una RNN, generalmente A B C e D sono impostate a mano, generalmente sono time-dependant

## Continuous State Space Model

Immaginiamo di avere un auto che abbia una certa massa, ha una posizione x1 a tempo t ed una x2 che è la velocità, le chiamo x1 ed x2 perchè costituiranno il mio stato, m è una costante, x1 ed x2 sono indispensabili per modellare il morto nel tempo, posso fare ragionamentoi per capire dove si troverà la macchina nel futuro e che velocità avrà. u(t) mi stabilisce il modo in cui il sistema si evolve nel tempo, la forza della frizione è proporzionale alla velocità, il mio stato lo chiamo x(t) è un vettore interpretabile dove il primo numero è la posizione il secondo numero è la velocità.

Per costruire il mio State Space Model ho bisogno delle derivate, dico quindi che x primo di t è uguale ad x2 di t, la derivata della posizone dello spazio rispetto al tempo è la velocità. Mi serve la derivata di x2, sappiamo che la derivata della velocità rispetto al tempo è l'accelerazione, possiamo dire che la massa è uguale alla forza per l'accelerazione, da qui ricaviamo che x2 primo di t è uguale. 

Ho un sistema che mi dice le derivate di x1 e di x2, ho di fatto quello che mi serviva ho espresso la derivata in una funzione lineare dello stato.

Se volessi fare un po di conti escono fuori delle matrici, andando a fare queste somme mi esce fuori un vettore che ha questi due elementi, il mio output è la posizione posso andare selettivamente a prelevare la mia posizone, la moltiploco per x e questo fa si che seleziono solo il primo elemento. Ho definito un modello che è timeinvariant che i permette di predire la posizione della mia macchina in qualsiasi istante temporale, lo stao deve essere noto a qualsiasi tempo t. In questa maniera riesco a predire tutto il resto, non è casuale ma è ingegneristica, se io premo sull'acceleratore.

L'idea è apprendere A B C e D, x(t) è il mio stato interno della rappreserntzione u(t) è una sorta di riassunto, questa cosa che ci siamo detti non funziona se non discretizzo, u ed y sono la mia supervisione, sono valori discreti? Sono continui, sono delle funzioni u ed y sono delle funzioni nel tempo, posson andare a derivare analiticamente, quando integro ho bisogno di trovare la primitiva.

## Discretized Space State Model

Assumo che la derivata di x è uguale ad una qualche funzione di t e di y, voglio calcolare se discretizzzo y di t1, prenderò valori di t discreti mi chiedo cosa succede a tempo 1 2 o 3 ma non mi chiederò mai cosa succede a tempo 1.5 o 2.4. Adcesso ho un numero finito e numerabile di input e di output discretizzando delta t, nel caso dei video ad esempio prendo 25 o 30 frame, nel caso dell'audio prendo una certa frequenza, nelle immagini discretizzo sia nello spazio che nelle frequenze.

Se ho una funzione continua e ne conosco la sua derivata, vorrei calcolare questo valore mi muovo lungo delta t, voglio trovare il valore della derivata senza avere un valore analitico ho un valore che è stato campionato ho una costante che è un valore discreti che è una formukazione analitica, y di t1 è uguale a y di t0 + f(t0, y0)(t1-t0). 

La derivata mi dice come cambia il valore di quello che sto considerando a variare del tempo, moltiplico per delta t, ho un cambiamento di 8 assumo che il mio modello sia lineare, se prima mi trovavo al valore 2 farò 2 più 2 per due 4 spostandomi quindi da 2 a 6, anche in questo caso posso vedere tutto in termini di matrici, se io imposto A B C, mentre D=0, C non ha bisogno di essere discretizzata, A e B che hanno a che fare con le derivate devo discretizzarle. Le versione discrete di A e B dipendono analiticamente dalle versioni continue di A e B, perchè è interessante? X a tempo k+1 è uguale ad x a tempo k per qualche matrice anche non interpretabile per Bd per uk è molto simle a quanto visto nelle reti ricorrenti, se pensiamo alle RNN prima di LSTM funzionano esattamente così. Ci siamo arrivati passando attraverso una funzione continua. Immaginiamo di stare analizzando dell'audio costruisco un modello che funziona su campioni discretizzati ma ho un modello continuo, qual è il vantaggio di avere un modello continuo? Non devo campionare, è più di alto livello, posso prtendere gli istanti in mezzo, immaginiamo che sia riuscito ad apprendere A B e C, voglio discretizzare e ragionare su una frequenza di camionamento a 44100 kz, imposto delta t a 44100 hz, sarebbe 1/44100 secondi, se volessi lavorare ad una frequenza diversa cambio delta t ma non cambio A B e C, posso utilizzare i miei campioni a tempo delta t nello stesso modello perchè hanno le stesse matrici continue. Dato che Ad e Bd dipendono da A e B non ha senso apprendere queste matrici, vorremmo apprendere A e B, lascio statica la funzione di discretizzazione, faccio training con una certa frequenza di campionamento, prendo altri campioni, questo mi permette di fare training con una determinata sequenza di campionamento e test con un'altra sequenza di campionamento. Ho un modello che mi permette di essere discretizzato in infinite maniere a seconda dei miei usi. 

Posso imparare A e B supervisionando con qualcosa di discreto, per delta t = 0.1 somiglia ad un modello continuo, vorrei imparare le matrici A B e C. 

## State Space Models vs RNNs

Quello che abbiamo ottenuto è qualcosa che si avvicina molto alle RNN, il modello è lo stesso ma perchè ho fatto tutta questa fatica per arrivare qui questo mi permette di trovare le versioni continue che posso discretizzare come voglio.

## Neural State Space Models

Non ci interessa la questione matematica di come ci si è arrivati, il sistema a continuous time è il sistema del moto deterministico che abbiamo visto prima, ho in input l'accelerazione lo moltiplico per una matrice e questo mi permette di definire una funzione analitica, se applico più forza la mia velocità aumenta, devo fare un operazione di integrazione, se io discretizzo ottengo una formulazione che somiglia quella di una RNN standard in maniera discreta prendo degli input u, passo da uno spazio continuo ad uno spazio discreto input ed output, apprenderò le versioni continue ed in termini di implementazione creo una matrice di parametri allenabili la passo al mio optimizer, la A discreta come la ottengo? Discretizzando A, nel caso di Eulero è una funzione differenziabile, i miei gradienti passeranno attraverso A, posso fare un delta t che posso imparare affinchè funzioni bene nel mio passo di discretizzazione. Di fatto sto imparando una RNN, quando ho finito il training prendo le mie versioni discrete.

## State Space Model Computation

SSM detto S4 è uno dei modelli che in realtà non funziona, ho in input x ed in output y, l'input x ha una shape (B,L,D) vado a definire A di dimensioni DxN proietto dalla dimensionalità D alla fimensionalità dell'hidden vector N. Ho x(t) moltiplicato per B ed ho un Ah(x) h'(t) = Ah(t) + Bx(t).Y(t) = Ch(t).

B mi deve permettere di proiettare, C mi permette di proiettare da N a D, dipende da dove metto i trasposti, dopodiche definisco delta uguale ad un parametro che ha dimensione D, posso discretizzate ogni componente del mio segnale in modo diverso, le ottengo mediante una funzione di discretizzazione y=SMM(A,B,C) dove SSM è uno state space model, l'applicazione delle versioni discrete di queste due.

h[k+1]= Ah[k]+bx[k]
Yt[k+1]=ch[k]

## Gli SSM sono semplicemente lineari?

Se apprendessimo in modo lineare otteniamo modelli che non sono molto performanti, utilizziamo i transformer che per come sono fatti non sono lineari, ottengo combinazioni differenti, prendo un SSM lineare, metto una non linear activation, all'interno del SSM se io lo srotolo nel tempo è un rapporto lineare, il rapporto cross layer non è più lineare, il modello non è più predicibile o analitico, troverò qualcosa che ha senso ma non ho garanzie teoriche tra input ed output.

## Ricorsione, è lenta?

Dato  che i modelli sono lineari è più facile da parallelizzare, lo faccio tramite una vista a convoluzione degli SSM, ho questa equazione di x con k, e di y con k che è una ricorrenza. Allo step 0 x0 = B di u con 0, allo step 0 avrò anche che y con 0 = CB per u con 0, C non va discretizzata. Allo step 1 avrò x1 che è Ax0 + Bu1 = ABu0 + Bu1.

Andando avanti per x2 ottengo A quadratico. In generale quello che esce fuori è che y al passo k avrò C per A alla k, per B per u con 0 più tutti gli altri termini che vengono dalle precedenti iterazioni. Ho tutti i miei input moltiplicati,, è una combinazione lineare degli input con dei pesi che sono diversi e precomputati, A B e C sono sempre gli stessi non variano nel tempo, come li precomputo? In maniera iterativa. Posso vederlo come se fosse una convoluzione, dove u è una sequenza k è un vettore, esiste un moldo efficiente di precalcolare queste matrici, trasformo una sequenza lenta in convoluzioni implementate in maniera efficiente in CUDA. 

## Copying, Selective Copying and Induction heads

MAMBA si pone il problema di fare copia selettiva degli input, ho un certo input e devo iniziare a copiare tutto quello che avviene prima di un certo evento, tutto quello che vedi prima del token nero, questo tipo di task è fondamentale nel natural language processing questo tipo di task dimostrano che modelli come S4 non lo possono risolvere perchè A B e C non dipendono dall'input, sono gli equivalenti dei pesi di attenzione che ho calcolato.

MAMBA fa in modo che A B e C dipendano dall'input, prendono x mediante un layer di proiezione lo trasformano In B C e delta t e diventano impllicitamente dipendenti d t, anche il parametro di discretizzazione dipenderà da t. 

## The Selection Mechanism

L?algoritmo MAMBA detto S6, B lo ottengo prendendo x, lo espando in tante matrici una diversa per ogni elemento in modo tale che ogni elemento predice la sua matrice B la sua matrice C ed il suo fattore di discretizzazione diverso per ogni stato temporale. L'idea di MAMBA è far dipendere tutto da t, a seconda degli input che ottengo psso cambiare il modo in cui adatto il mio hidden state, gli input definiscono il comportamento del moodello, è importante che l'output dipenda dall'input motivo per cui fanno dipendere A B e C dall'input in modo che possano cambiare. 