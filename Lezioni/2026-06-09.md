# Reinforcement Learning - Martedì 09 giugno 2026

Per capire il deep reinforcement lrearning, bisogna capire le basi del reinforcement learning. Abbiamo bisogno di fare un lungo preambolo di concetti di base, QLearning è la prima forma di reinforcement learning.

## Apprendere dai dati statici vs apprendere facendo

Ci chiediamo qual è la differenza tra self supervised learning  e reinforcement learning.

Nel learning supervisionato (e non) ho dataset preessitenti, snapshot della realtà congelati per sempre, richiede etichette esplicite, nel self supervised learning genero queste etichette dall'input o mi aspetto di ragionare su dei dati che non si evolvono nel tempo. Il processo di data collection è separato da quello del learning, si tratta di approssimare una funzione, chiaramente non sempre è dfacile acquisire questi dataset.

Il reinforcement learning cerca di affrontare il problema di procurarsi i dati in modo diverso, l'idea è che i dati vengano generati durante la fase di apprendimento con il paradigma Learning by Doing, apprendere mentre si fanno le cose, pensiamo a come si impara ad andare in bicicletta, facendo delle simulazioni imparando ad applicare la giusta pressione ed equilibrio, le impariamo provandoci tramite trial & error. Non è randomico, ma smart, provo e capisco cosa funziona e cosa non funziona. Tramite una iterazione contiinua provo a fare delel cose, ricevo un feedback dall'ambiente e cerco di migliorare il modo in cui sto affrontando il problema, fare errori fa parte del progresso mi aiuta a capire come migliorare.

## Applications: Where RL Shines vs. Supervised Limitations

Imparare ad apprendere physical skills, il reinforcement learning ha tantissime applicazioni in robotica, immaginiamo di addestrare un robot a prendere un oggetto, il supervised learning funzionerebbe solo se coprissi tutti i casi possibili il che è praticamente impossibile, bisogna cambiare paradigma, la soluzione è il reinforcement learning, interagendo con l'ambiente genererò lo stesso le mie osservazioni ed imparerò a recuperare dall'errore, genero dei dati e dei negativi che sono sulla mia soglia di apprendimento.

Nel gioco degli scacchi, potrei pensare di avere un dataset etichettato di partite, prendo la scacchiera e dico quale mossa fare con un approccio supervisionato, ma coprire tutte le possibili combinazioni della scacchiera è impossibile e se trovo una confgigurazione mai vist non saprò cosa fare, il reinforcement learning mi aiuta apprendnedo dagli errori commessi.

Anche nell'autonomous driving, ma non nel mondo reale ma in ismulazione, o ancora allineamento degli LLM per allinearli alle preferenze dell'essere umano, gli LLM generano una rispposta e gli esseri umani dicono "questa risposta la preferisco".

La differenza sta principlamente nel come vengono acquisiti i dati, nel RL li acquisico durante l'apprendimento, se il robot prende il telecomando e cade acquisirò tante acquisizioni fallimentari e posso imparare fin quando il robot non diventa più bravo.

## Gridword Example

L'esempio gridword, imamginiamo di avere una griglia dove abbiamo un robot che deve trovare l'uscita, e ci sono delle cose che deve evitare, noi vorremmo che il nostro robot impari il percorso migliore, supponiamo che si possa muovere solo in orizzontale o verticale, vogliamo arrivare all'uscita nel minor numero di sstep possibili senza cadere in trappole. L'approccio supervisionato corrisponderebbe a dare etichette ad ogni casella e capire cosa dare in output in ogni possibile combinazione, l'approccio RL è apprendere il migliore percorso tramite trial and error, il robot inizialmente si muoverà in modo del tutto casuale, quanso arriva in un punto saliente come il fiuoco lo faccio tornare al punto di partenza e gli do un segnale un rewaed che può essere sia positiva che negativa, se il robot arriva al punto di destinazione gli do un rewaed positivvo altrimenti se inceppa in delle trappole gli do un reward negativo.

Terminologia:
- Agente: robot che naviga la griglia
- Ambiente: griglia con trappole ed uscita
- Stato: St appartenente ad S grande, stato in cui si trova il mio agente in un dato momento temporale, possiamo vederlo come le coordinate dell'agente in un determinato  mmomento, ppuò contenere tante cose in AD può contenere info su velocità dell'automobile, orientamento, benzina, tutte infromazioni utili per capire quale aziuoneb iosgna fare
- Osservazioni: Parti dello stato che l'agente può vedere, non sempre lo stato è totalmente opsservabile, supponiammo che io abbia un sensore che mi dice esattamente dove mi trovo, l'immagine mi permette di risalirer parzialmente allo stato o alla cella ma le due cosde non coincidono
- Azione: Ad ogni istante temproale prenderò un azione diversa, possiamo pensare di fare uno spostamento phi, ragioniamo in spazi discreti che contiene le direzioni up right left down
- Reward: Le dà l'ambiente avrò una reward differente a seconda dell'azione intrapresa, invece di costruire un dataset, costruisco un ambiente con reward +1 se raggiugniamo il goal -1 se cado in una trappola e -0.01 per scoraggiare i percorsi troppo lunghi, viene assegnato ad ogni step
- Policy: Funzione definita in O con valori in A (osservazioni ed azioni) è una regola che dice all'agente in quale direzione muoversi in ogni cella, si prende l'osservazione e mi dice quale azione intraprendere se muovermi in basso alto destra o sinistra
- Ritorni: Sono dei segnali più forti delle reward che mi dicono qual è la reward che mi devo aspettare a tempo t, tutte le azioni hanno una reward negativa molto piccola, alcune ne hanno una molto grande (le trappole) tra due che hanno -0.01 quale delel due dovrei preferire? Quella più vicina all'uscita, ma ragioniamo in termini di reward, è quella che ha più chance di accumulare meno reward negative, facendo percorsi più lunghi avrò reward cumulativa superiore, G(t) viene detto ritorno ed è il reward cumulativo, oggi faccio un'azione ma cosa mi torna da quest'azione cosa ci guadagno? Simile al concetto di ricavo negli investimenti
- Credit Assignment Problem: Supponiamo che il mio robot faccia un percorso ed ottengo una reward positiva, ne faccio un altra ed ottengo una reward positivo, il primo percorso era più corto, il secondo era più lungo, se questi due percorsi hanno due reward diverse dovute alla loro lunghezza differente, ad ogni tempo t prendo un'azione differente, alcune azioni hanno un ruolo più o meno importante nel raggiugnimento del goal, ma alcune azioni sono state utili per il raggiungimento, altre sono del tutto inutili, non è facile capire quale azione è utile e quale no, questo è il problema di assegnamento del credito, è difficile capire quale azione ha il contributo maggiore nel raggiugnimento de mio obiettivo.

## The Reinfocement Learning Cycle

Tutto funziona in un ciclo, ho un agente che esegue un'azione ottengo uno stato ed un reward, al passo usccessivo ripeto lo stesso ciclo che dipende da S e T nei tempi precedenti. Questo è un loop continuo, non è sempre detto che l'agente abbia sempre accesso allo stato, nell'autonomous driving non ho uno stato completo a mia disposizione, per un sorpasso vorrei avere idealmente un modello 3d per capire che spazio ho a disposizione, in realtà ho solo un'osservazione a mia disposizione.

## Episodic vs Continuous Reinforcement Learning

Vedremp principalemte quello episorico, il mio apprendimento avviene in episodi che hanno un inizio ed una fine, quando arrivo ad una trappola ho una reward negativa, quando arrivo ad un goal ho una reward positiva, se non arrivo entro un determinato numero di step al goal interrompo il ciclo, mi fermo e faccio un apprendimento. Gli episodi generalmente sono infiniti

Nel caso continuo, come quello dell'autonomous driving, non ho un numero predefinito di step e nemmmeno un goal predefinito da raggiungere

## Goal and Returns

Per definire return, in generale anche nel caso continuo ma vale in quello episodico, definisco i return in maniera matematicamente ben definita anche se gli step sono mamtematicamente infiniti, il mio return è la reward che accumulerò da qui nel futuro, questo valore del return che identido con G lo posso definire come (formula nelle slide). Questo modo ha un paio di problemi, se gli step sono infititi G(t) tende ad infinito, non riesco a distinguere tra due percorsi che tenfono ad infinito, quando io mi trovo a tempo t so che l'azione che prendo al rempo t infgluenzaerà molto le azioni che avverranno a tempo t+1, t+2, t+3 ma influenzeranno poco le azioni che avverranno tra molti timestep. Localmente mi fa sopravvivere, ma globalmente non mi fa andare verso il goal, per pesare meno le reward del positivo facciamo una discounted cumukative reward, scontata con un fattore di socnto chiamato Gamma che è un vcalore tra 0 e 1 che uso per pesare di più le reward immediate e sempre meno le reward più lontane. All'inizio Gamma tenderà a 0 che viene moltiplicato per una reward di t+k+1 questa formulazione ha un paio di vantaggi, dal punto di vista matematico mi permette di non saturare ad infinito, ho un intuizione di tipo epistenica, influenza molto il presente ma sono incerto di come la mia azione influenzerà il futuro. Do uno sconro a quello che succederà nel futuro, gamma mi permette di capire quanto il mio agente guarda nel futuro o quanto guarda le cose vicine, con gamma che tende a 0 avrei un agente miope, il mio return dipende solo dalle azioni immediate, per gamma che tende a d1 sto pesando alla stessa maniera tutto, generalmente gamma si sceglie strettamente minore di 1. La definizione di return è quello che guida il reinforcmenet learning, se io vado avanti questa è la distribuzione del mio return

## Markov Decision process

Generalmente quando parlo di RL si introuce un concetto chiamato Markov Decision Process, è definito da una tupla che indica una serie di elementi matematici che definiscono un complesso teorico matematico che spiega gli episodi di reinfocement learning, posso parlare una lingua che è quella dell'ottimizzazione per massimizzare le reward.

S è lo spazio degli stati, nel caso del mondo a griglia S potrebbe essere l'insieme delle possibili celle, l'id della cella o le coordinate della cella, A è lo spazio delle azioni su giu destra sinistra, P è il modello di transizione, s' è lo stato nuovo, sè lo stato vecchio ed a è l'azione che sto intraprendendo, se io mi trovo in questo s ed intraprendo un azione a qual è lap orbabilità di trovarmi in un altro s'. Modello probabilsitico e non detemrinistico perchè ho sempre un certo grado di incertezza. Nell'autonomous driving  io ho un modello che mi dice se svolti a destra, vai nella strada a destra, un pedone non riusciamo a modellarlo, potrebbe interferire con il mio stato se attraversa nonostatnte il semaforo rosso renderebbe impossibile usare l'approccio deterministico. In un caso a griglia potremmo usare una distribuzione con entropia nulla le probabilità saranno sempre 0 o 1.

R è la funzione di reward, non necessariamente la dobbiamo modellare, mi da una reward a seconda di dove mi trovo nell'mbiente. Gamma è il fattore di sconto, a seconda di come modifoco Gamma scambia il modo in cui il mio agente si comporterà. Si chiama modello markoviano perchè è valido il modello di markov, mi dice che la probabilità di andare in st dato lo stato st-1 e l'azione a dipende solo dallo stato in cui mi trovavo, anche se metto dentro le cose vecchie st-2, st-3 sarebbe lo stesso risultato, questa assunzione non sempre è corretta, nel caso degl iscacchi la probabilità di trovarmi in uno stato se prendo una determinata azione non dipende dagli stati precedenti, In altri casi posso definire un nuovo spazio degli stati dove gli stati contengono anche le traiettorie.

Si parla spesso di modelli markovianoi parzialemtne osservabili (POMDPs) sono modelli in cui non osservo totalemtne lo stato ma lo faccio tramite osservazioni che seguono una certa probabilità seguendo una qualche distribuziuone di probabilità non ragiono sugli stati ma dalle osservazioni, definisco il modello in maneira analoga.

Parlaimo di policy in maniera probabilistica, io generalmente ho una funzione definita in valori [0,1] se io mi trovo nello stato s qual è la probabilità di intraprendere l'azione a, mi dice quanto è vantaggioso prendere l'azione a nello stato s, potrei scegliere l'azione ottimale usando la funzione argmax prendo la a che mi massimizza la probabillità, nello spazio discreto la probabilità è un vettore di 4 elelemnti che sono compresi tra 0 e 1 e faranno somma ad 1. potrei definire una policy come una rete neurale che prende in input un immagine e restituisce 4 elementi che sono le probabilità di effettuare determinate azioni, non ho accesso diretto allo stato, la mia policy  mi da un vettore di probabilità faccio argmax e mi sposto lì.

## The Credit Assignment Problem

Supponiamio di fare una partita a scacchi resta il problema di asegnamento del credito, non sappiamo quali mosse mi hanno portato a perdere o quali a vincere, i problemi sono le reward ritardate non le ottengo ogni volta che faccio una mossa ma le ottengo solo alla fine, posso avere delle reward disperse e quando abbiamo lughe sequenze di azioni.

## Value Functions and Q-Functions

La value function è la funzione dello stato, ci chiede quanto  è buono trovarci nello stato s seguendo la policy, dipende da entrambe le cose, nel mondo a griglia la value function mi dice qual è il valore assumendo la policy, la domanda alla quale ceco di rispondere è qual è il valore di questa cella? Supponiamo di avere una policy ottimale che mi porta sempre al goal, quale può essere un mod oragionevole per calcolare questo v, potrebbe essere la distanza ma dipende dallo topolgia del mio mondo, in generale dovremmo utilizzare i termini che abbiamo già visto, la reward cumulativa, il valore che ho qui è il return, ma abbiamo introdotto un modello markoviano la policy è non determinstica, piuttosto che dire che io qui assegno un G(t) prenderò una media di tutti i miei possibili G(t) partendo da quello stato. Come la calcolo? Il valore atteso è una media, non è determinstico quindi prendo il mio mondo a griglia mi metto nel punto S seguo la policy e vedo dove arrivo, faccio questa operazione 1000 volre calcolo la media e questo è il mio valore di S. Vado al punto di partenza prendo tutte le traiettorie che sono passate da S, a predscindere da come arrivo ad S in media che return ottengo? Più mi trovo vicono al goal più mi aspetto che il reward sia alto, se qualcuno arrivasse e ci desse i valori ci farebbe un grande favore, se dal mio stato posso vbedere i valori delle celle adiacenti posso decidere di spostarmi verso la cella che ha il valore più alto, ma questa quantità non è correlata all'azione che voglio intraprender perchè dipende dalla policy, trovo la cella che ha il valore più alto ma la mia policy mi dice di non prenderla, quindi dovrei scartarla o cambiare la policy è una contraddizione. Per risolverla introdicuamo la Q Function, supponiamo di avere una policy ci chiediamo qual è il valore che ottengo seguenndo lo stato s e la policy p, è uguale al valore ma mi permetto di cambiare la prima azione questo va a parare il fatto che questa azione va contro la mia policy, ho una policy che mi permette di muovermi che mi porti verso l'obiettivo e che mi dia degli errori per capire cdosa non fare. Il valore di s è uguale al Q Value di S(a) moltiplicato per la policy e sommato su tutte le altre, non è altro che una marginalizzazione, ho una quantità che dippende sia da S che da A, dipende dal valore del mio stato moltiplicato per la probabilità di arrivare in quello stato

Calcoliamo v e Q faccio tanti episodi, quando mi trovo nello stato A, questi episodi che reward cululativa hanno ottenuto faccio una media di queste e rappresenta il metodo montecarlo, posso apprendere delle policy, è chiaro che se io ho il valore q, mi trovo nello stato s qualsiasi esso sia, un modo per fare reinforcement learning è prendo il mio agente esploro in maniera ranodm ottengo i miei Q Values, ottengo una tabella di valori e come policy prendo l'argmax di st,a. Ho una policy greedy

Le optimal value functions significa che risolvere un problema di reinforcmenet learning significa trovare una policy che riceverà delle buone reward, si utilizzano spesso degli oggetti matematici i valori ottimali sono quelli che ottengo quando scelgo la policy che mi da i valori maggiori prendo la policy che mi da il valore maggiore, in teoria esiste una policuy che mi massimizza tutti i valori di V e tutti i valori di Q idealmente se io prendo una policy random sono i valori ottimali o sono quelli di una dewterminata policy? 

## The Bellman Equation 

L'eqauzione di bellman mette in reralzione il valore di uno stato con il valore di un'altro stato, ci dice che il valore dello stato S seguendo la policy pi è uguale alla media della reward che ottengo a tempo t+1 + il valore dello stato di arrivo s' al quale arrivo scontato per gamma, questa è la media quando st = s.

Io mi trovo in uno stato s al tempo t prendo un azione a che segue la policy pi ( . | s) quando faccio questo passo vado in un nuovo stato s' ottengo una reward il valore non è la reward immediata che otteengo ma va sommata alle reward che otterrò nel futuro, vado nel prossimo stato vedo qual è il valore del prossimo stato e dirò che il valore è sommato al valore dello stato immediato più quella futura. 

A questo punto essendo una equazione stocastica devo fare una media, ma è ricorsiva non devo applicare più montecarlo invece di prendere una serie di traiettorie aggiorno il valore dello stato corrente con una stima dello stato successivo, se seguiamo l'equazione di bellman il valore converge al return, posso scriverl ocome sommatoria e questa cosa è esattamente uguale ai return. L'equazione di bellman è semplice ma muove tutto il reinforcement learning, posso aggiornare i mei valori in ogni momento afd ogni passo, le posso applicare anche nel caso dei Q Values, i valori Q ed i valori V hanno formulazioni simili 

## From Monte Carlo to temoral difference learning

Il temporal difference learning usa l'equazione di bellman, v di st viene aggiornato con vst con alpha che moltiplica r + gammav - v, se ricordiamo l'equazione di bellman supponaimo che la mia policy sia determinstica posso buttare via l'expectation e dire che v di pi di st è uguale ad r con t+1 + gamma di v(st+1). Ci dice che dovrei aggiornare st con l'espressione che vedo, potrebbe non essere una soluzione molto stabile, se io ho una inizializzazione random o uniforme di v le reward predominerebbero, vado a smorzare questo update utilizzando un learning rate, prendo il mio valore vecchio e gli sommo il valore nuovo meno il valore vecchio, se faccio così è del tutto equivalente ad una sostituzione, quindi metto un gamma davanti affinchè moduli l'apprendimento per gamma = 0 non cambio nulla per gamma = 1 sto esattamente sostituendo per gamam tra 0 e 1 aggiorno il valore vecchio con quello nuovo ritenendo parte del valore vecchio per evitare che la stima cambi in modo radicale tra un updfate e l'altro.

## From Evalutation to Control

Data una policy posso trovare dei valori V e dei valori Q, se io ho tante possibile azioni posso applicare il temèporal difference learning e vado ad aggiornare il mio q ottimale seguendo l'azione migliore secondo il mio vecchio valore di Q posso andare ad applicare lo stesso meccanismo utilizzando una policy greedy, scelgo l'azione migliore che massimizza q ed aggiorno q ottimizzando la reward. Qui dico che invece di seguire la policy pi prendo tutte le mie possibili azioni e faccio max mi permette di serguire la policy migliore per aggiornare q.

## Tabular Q-Learning

Una formulazione concreta è quella del tabular q learning, io inizializzo i valori q in maniera generalmente uniforme ed utilizzo il temporal differece per aggiornare questi valori, il nuovo valore di q è uguale al vecchio valore di q + formula delle slide, il massimo mi dice di seguire sempre la policy che mi da il q massimo. iniziamizzo le q in maniera random o continua inizializzando tutto a 0 andrò inizialmente a caso man mano aggiornerò le mie q muivendomi sempre con policy aggiorante.

## The exploration expolitation dilemma

generalmente la mia policy ottimale non riesce a farmi vedere corner case, imparo la policy anche se ne seguo un altra dentro la formula trovo la policy ottimale ma quando mi muovo nel mondo a griglia scelgo una policy epsilon greedu se episoln è un numero tra 0 e1 per una percentuale di epsilon volte sceglie a caso.
