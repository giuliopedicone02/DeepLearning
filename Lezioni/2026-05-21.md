# Advanced Sequential Modelling - Giovedì 21 maggio 2026

Andiamo oltre i transformers e gli LSTM, le sequenze possono essere frame estratti da video, sequenze di flussi audio. Le sequenze sono dappertutto e tutto può essere visto come una sequenza, quando abbiamo visto le CNN non vedevamo le immagini come una sequenza di pixel o di patch, è un architettura general purpose, questo ci porta alle metodolgie che ci permette di rappresentare vettori di rappresentazione

## What about convolutions?

Le CNN ci permettono di analizzare delle immagini, ma le immagini sono sequenze di pixel quindi perchè non utilizzare le convoluzioni? Possiamo pensare di utilizzare le convoluzioni per sequenze unidimensionali, è l'idea che sta alla base delle Temporal Convolutional Network

## Temporal Convolutional Network

Prendo un insieme di task da fare su sequenze di dati, le analizza e affronta questi problemi con LSTM e TCN e per tantissimi task le TCN sono molto più competitive delle LSTM.

Come è fatta una TCN? Il mio input è una sequenza di vettori, se le mie sequenze sono dei vettori o possiamo immaginare che sia uno scalare, prendo localmente faccio padding con 0 e faccio una convoluzione che è una combinazione lineare, il kernel non è bidimensionale o tridimensionale ma è monodimensionale, vado avanti così ho una sequenza iniziale ed ottengo una sequenza in uscita, possiamo dire che x1 dove 1 indica il layer è uguale ad x al layer 0 convoluzione con layer 0. Vado avanti su più layer come una CNN al layer 2 ho x2 = x1 convoluzione per layer 1 e vado avanti così, ho un modello che data una sequenza mi permette di mappare una sequenza. Alla fine ottengo un sistema sequenxe to sequence parametrico dove i parametri sono i pesi dei kernel e ci aspettiamo una funzione di attivazione non lineare e che sia altamente parallelizzabile, non posso parallelizzare i layer  ma all'interno dello stesso layer la convoliuzione è parallela, una RNN avrebbe dovuto computare in modo sequenziale, uno svantaggio qui è quello di non avere una menoria espliocita, è un modello non causale o modello non online, devo bufferizzare le mie attivazioni, le RNN hanno un vantaggio enorme comprimo tutto in unico vettore che occupa uno spazio in menoria fisso e non ho bisogno di bufferizzare. Inoltre il receptive field cresce nelle TCN, propagando nel giro di pochi layer ciascuno di questi elementi abbia visto tutti gli elementi della sequwnza di partenza, è un vantaggio volendo. 

## Depth and Sequence Length

Avere tanti layer è un arma a doppio taglio generalmente quello che si fa è guardare la formula che ci dice che d è il numero di layer, k è la dimensione del kernel e a variare di L e k ci dice che se L è 100 e k è uguale a 3 ho bisogno di una profondità di 30 layer, la soluzione si chiama dilated convolutions

## Dilated Convolutions

Il mio dilaction factor aumenta al vartiare del numero di layer, se applico questa formula il che se vedo tutti i frame otterrei 6 layer, a d=2 sto saltando alcune cose, a d=3 ne salto 2, il receptive field mi permette di bvedere nel giri di pochi layer tutta la sequenza, è vero che sto saltando alcune cose ma all'interno delle sequenze unità vicine spesso hanno informazione ridondante, il valore che ottrengo a tempo t è molto simile a quello che ottrengo a t+1

## TCN and online processing

Questo tipo di connettività non è buona per fare ragioanmenti online, uno dei primi task è stato quello di fare speech recognition ma vorremmo che tutto sia online, lo sppech to text vorremmo farlo online non vorremmo che finisca l'audio, ci serve quindi un buffer, vogliamo che le decisioni a tempo t dipendano da quanto successo a tempi t primo < t. Posiamo pensare di tagliare delle connesioni che sono quelle che guardano verso il futuro, a test time le possiamo togliere, le restringo le tolgo a training time e questo assomiglia ad una tecnica che utilizzano i transformers, il masking è una forma di masking.  

## Dilated Casual Convolutions

Il frame più recente non lo salto mai è l'informazione più preziosa che ho, vedo le unità passate ed è comèpatibile con scenari streaming o online che non guarda nel futuro mi aspetto che il mio flusso di dati non finisca mai quando parlo di streaming, posso pensare di usare un kernel più piccolo e fare uno shift. Il receptive filed ci permette di guarsare solo al passato en on al futuro

## Dettagli architettueali

Vedi figura e descrivi.

In pratica possiamo implementarle in diversi modi cambiando funzioni di attivazione, il numero di layer , le connessioni residurali, su diversi task le TCN spesso ottengono migliori performance rispetto ad LSTM, GRU ed RNN. Tutt'ora sono competitive anche rispetto ai transformers.

## Multistage TCN for action segmentation

Rappresenta tuttora lo stato dell'arte ho in input dei frame li passo ad una TCN vorrei creo una rete multistage dove alla fine di ogni stage c'è segmnentazione, ho un blocco di convoluzioni e metto una cross entropy loss per mettere le mie azioni in output, faccio refinement, mettendo i bottleneck funziona meglio rispetto ad una TCN vanilla e generalmente sono più veloci da allenare e da testare.

## Ma l'attenzione non era tutto ciò che basta?

Non posso utilizzare i transfomrer per risolvere quersto tipo di problema? Laddove dove i transformers non sono molto adatti sono i casi in cui hoi bisogno di fare processing online 

## Possono essere usati i transformers online?

Si possiamo utilizzarlo usando il casual masking, faccio un masking in modo tale da ottenere degli inputdegli output, ma come faccio  a fare questa cosa man mano che mi arrivano gli input? dal primo input calcolo il primo output facendo self attention, quando arriva il secondo input faccio cross attention con diverse combinazioni ma ripeto in realtà un sacco di combinazioni, non ha senso e quello che generalmente si fa è sare una KV Cache so che key e value mi serviranno dopo li metto dentro una cache calcolo l'attenzione, prendo una query nuova prendo key e value li metto dentro la KV Cache e faccio la stessa cosa, dentro la KV Cache ho tutti i miei valori precomputati che mi permettono di calcolare ogni bolta una riga o una colonna nuova, non è un operazione onerosa ma è ottimizzata rispetto a rifare N operazioni. Il problema è che la kv cache cresce nel tempo e anche la computazione. La memoria è qualcosa che le forme di vita hanno costruito per sopravvivere è uno spazio compressivo che mi permette di avere continuità nel tempo, anche alcune piante hanno memora è una caratteristica intrisncca degli esser iviventi. 

## Transformers for online sequence processing

Ho una sliding window con meccanismo FIFO mi permette di avere una dimensione della KV Cache contrained ed ogni volta che ho un nuovo token non crescerà, non vorremo che qualcosa online diventi sempre più lentro nel tempo, i chatbot comprimono il contesto fanno ujn summary per sumuaklre questa dimensione finita o limitata della memoria, posso mantenere il class token rappresenta l'approccio vanilla tutto quello che è fuotri dalla sliding window la sto eliminando.

## LSTM for online action detection

L'idea è che io prendo un frame alla volta l'azione avviene in maniera online man mano che i frame arrivano

## Long Short Term Transformers

Vorrei voler dimenticare delle cose e ricordare delle altte prendo un buffer e lo divido in due parti, short term memory e long term memory, sulla shorter memory faccio quello che fa normalmente un transfoerms guardo tutto nella longer mmeory faccio compressione. Sui frame che si trovano prima della long term aggiungo positional encoding e faccio un layer decoder che ha delle learnable queries le inizializzo con nn.parameters che imparano a riassumere una lunga sequwnza in una dimensionalità fissa, ho 100 elelemti prendo 10 learnable queries, ho 100 elementi in input calcolo le mie keys e values ma le mie query le calcolo su 10 elelemti, sono sempre il numero di queries in output. 

Ho due query, tre keys e mi calcolo una matrice di attenzione cross attention che avrà dimensione 2x3 il numero di values che devo avere deve essere uguale al numero di keys, v1 lo moltiplico per il primo elemento della prima riga, v2 lo moltiplico per il secondo elemento della prima... li sommo ed ottengo V1

La matrice di attenzione sarà sempre numero di queries per numero di values, la dimensione della amtrice di attenzione sarà m con l per m con 0, prendo un layer con una matrice hgrossa e tanti layer con matrici piccole riassume la sequewnza in una più breve, utilizzo come query la shorter memory, quello che esce è una memoria e decodifico facendo il decoding ottengo un altra sequenza l'ultimo di questi elementi sarà quello che passo alla mia cross entropy per fare classificazione.

Le query sono i parametri del modello i frame diventano due vettori una versione compressa dei 100 frame, li utilizzo per fare predizione con un altro transformer.

Il primo approccio è fare classificazione faccio attention su tutta la mia sequenxa, faccio una matrice nxn che ha i problemi che abbiamo visto prima, il ragionamento è comprimerne alcuni utilizzo ad esmepio un transformer, prendo un layer di transforrmer e li faccio passare attevaeeso un layeer di transformer che comunque avrebbe la stessa lunghezza, il trick è che ciò che ho in output è uguale al numero di query faccio cross attention e scelgo ad esempio 3 query, come le prendo? Le query le dobbiamo apprendere, nel tempo le imparo in modo che l'output sia un utile, quello che ottengo sono 3 query che hanno fatto cross attention con tutto sono quindi dei summary.La long term è molto più piccola e compressa, uso un altro transformer, salto il blocco centrale nell'ultimo blocco ho la long term la interrogi per rislvere il mio problrma, classificare, lo faccio con un transformerm in output usciranno sempre 3 query, dei 3 prendo uqello più recente ed applico la mia loss function. Nel mezzo faccio fattorizzazione per risolvere problemi di computazione.

## I transformers sono RNN

L'idea di questo paper è fare un recap di come funziona un transformer, la standard attention ci permette di calcolare Q, K e V e ci permette di calcolare V' come softmax, questo è equivalente all'equazione sulla destra.

Se cambiamo la forma della similarità invece di avere l'esponenziale avessimo un kernel esponenziale in funzione delle proiezioni dell'input, ho generalizzato, se premdiamo un kernel che posso calcolare per phix e phiy la mia similarità non la calcolo più con l'esponenziale mappo tutto in uno spazio diverso se sono vicini nello spazio sono simili tra di loro, potrei avere delle proproietà mamtematiche più interessanti che mi aiutano, cosa notiamo? Pensiamo a delle semplificazioni, ci sono cose che non dipendono dall'indice della sommatoria dato che qi non dipende da j la prendo e la porto fuori, questo ha un vantaggio che ho disaccoppaito la query dai valori, se ho n prodotti qua ho delle sommatorie con un solo prodotto.

Gli autori dicono che come phi di x prendo elu di x +1

## Causal Masking

Quando faccio casual masking j che va da 1 a n va da 1 a i

Posso definire una ricorsione, il transformer diventa quindi una rete ricorrente. s:i è il termine che ho il numeratore z_i è un termine du normalizzazione che mi evita che la computazione esploda.